<UserControl
    x:Class="FluidIrc.Controls.ServerCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FluidIrc.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:ServerCardViewModel}"
    d:DesignHeight="75"
    d:DesignWidth="100">

    <Grid Padding="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding ServerName}" FontSize="24" />
        <TextBlock Grid.Row="1" Text="{Binding ServerAddress}" FontSize="12" />

    </Grid>
</UserControl>
